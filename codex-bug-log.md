# Codex Bug Log

## 2026-06-20

- Resolved 2026-06-21: `npm run build` failed before Next.js compilation in the clean `main` worktree because Prisma could not resolve `DATABASE_URL`. Fixed by giving `prisma.config.ts` the same localhost development fallback already used by `lib/prisma.ts`. Verified `npm run build` now reaches and completes Next.js compilation/static generation without a checked-in `.env`.
- Resolved 2026-06-21: `npx next dev -p 3017` failed in the clean `main` worktree with a Turbopack panic: `Symlink [project]/node_modules is invalid, it points out of the filesystem root`. Fixed the repo-supported path by changing `npm run dev` and `npm run build` to pass `--webpack`, and refreshed the main worktree install so `node_modules` is no longer a junction to the other worktree. Verified `npm run build` reports `Next.js 16.2.9 (webpack)`.
- Resolved 2026-06-21: `npx next dev --webpack -p 3017` started, but the shared `/admin/modules/[moduleId]` route compile did not finish; a direct request to `/admin/modules/products` timed out after 45 seconds. Fixed by replacing the generic module-page import context with explicit loaders and adding a concrete `/admin/modules/products` route that imports the Products module directly. Verified the route compiles as `/admin/modules/products` instead of the shared route; after compile, repeated requests return quickly. Browser/UI verification is still blocked by local database state in this worktree (`prisma.tenant.upsert()` returns a local Prisma 500), not by route compilation.
- Resolved 2026-06-21: dependency audit follow-up from the related main-worktree notes. Updated `next` to `16.2.9`, `nodemailer` to `9.0.1`, and added npm overrides for patched transitive `@prisma/dev`, `esbuild`, `postcss`, and `undici` versions. Verified `npm audit` reports `found 0 vulnerabilities`.
